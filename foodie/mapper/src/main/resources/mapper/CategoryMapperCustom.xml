<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.cheesun.mapper.CategoryMapperCustom" >

  <resultMap id="categoryVO" type="com.cheesun.pojo.vo.CategoryVO">
    <id column="id" property="id"/>
    <result column="name"  property="name"/>
    <result column="type"  property="type"/>
    <result column="father_id"  property="fatherId"/>
    <collection property="subCatList" ofType="com.cheesun.pojo.vo.SubCategoryVO">
      <id column="sub_id" property="subId"/>
      <result column="sub_name"  property="subName"/>
      <result column="sub_type"  property="subType"/>
      <result column="sub_father_id"  property="subFatherId"/>
    </collection>
  </resultMap>

  <select id="getSubList" resultMap="categoryVO" parameterType="java.lang.Integer">
    select
      f.id,
      f.name,
      f.type,
      f.father_id,
      c.id sub_id,
      c.name sub_name,
      c.type sub_type,
      c.father_id sub_father_id
    from
      category f
    left join
      category c
    on
      f.id = c.father_id
    where
      f.father_id = #{rootId}
  </select>

  <resultMap id="myNewItemsVO" type="com.cheesun.pojo.vo.NewItemsVO">
    <id column="rootCatId" property="rootCatId"/>
    <result column="rootCatName" property="rootCatName"/>
    <result column="slogan" property="slogan"/>
    <result column="catImage" property="catImage"/>
    <result column="bgColor" property="bgColor"/>

    <collection property="simpleItemList" ofType="com.cheesun.pojo.vo.SimpleItemVO">
      <id column="itemId" property="itemId"/>
      <result column="itemName" property="itemName"/>
      <result column="itemUrl" property="itemUrl"/>
    </collection>
  </resultMap>
  <select id="getSixNewItemsLazy" resultMap="myNewItemsVO" parameterType="Map">
        SELECT
            f.id as rootCatId,
            f.name as rootCatName,
            f.slogan as slogan,
            f.cat_image as catImage,
            f.bg_color as bgColor,
            i.id as itemId,
            i.item_name as itemName,
            ii.url as itemUrl,
            i.created_time as createdTime
        FROM
            category f
        LEFT JOIN items i ON f.id = i.root_cat_id
        LEFT JOIN items_img ii ON i.id = ii.item_id
        WHERE
            f.type = 1
        AND
            i.root_cat_id = #{paramsMap.rootCatId}
        AND
            ii.is_main = 1
        ORDER BY
            i.created_time
        DESC
        LIMIT 0,6
    </select>
</mapper>